## Setup

To install the required packages, run the following command:

```bash
pip install -r requirements.txt
```

This command will install all the packages listed in the `requirements.txt` file at once, ensuring that you have the necessary dependencies for the project. Please ensure you have the correct version of pip installed before running this command.